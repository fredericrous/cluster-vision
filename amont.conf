# duro doctor: @duro-app/ui build wiring (runtimeInjection, stylesheet
# import and CSS layer order). Scope = the files it reads in this repo.
pre-commit  duro-doctor  *.css,babel.config.js,package.json,root.tsx,vite.config.ts,vitest.config.ts  block  npx -y @duro-app/cli@^3.4.0 doctor
